[package]
name = "stella_frontend"
version = "0.2.0"
edition = "2021"
description = "Language frontend, AST, lexer, arena bump parser and symbol resolution for Stella Synaptic DSL"

[dependencies]
bumpalo = { version = "3.20", default-features = false }

[features]
default = ["std"]
std = []
